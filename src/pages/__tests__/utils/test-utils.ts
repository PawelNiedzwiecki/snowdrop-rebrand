import { type PageProps } from 'gatsby';

// Add this mock props object at the top of the file, after the imports
export const mockPageProps: PageProps = {
  path: '/',
  uri: '/',
  location: undefined,
  params: {},
  data: null,
  pageResources: undefined,
  serverData: null,
  children: undefined,
  pageContext: undefined,
};
